
@{
    Layout = null;
}

<style>

    th{
        text-align:center;
    }
</style>


<div ng-controller="ListOrderController as vm" class="container-fluid">
    <div class="breadcrumb">
        <h3 class="text-success ">Danh sách đơn hàng</h3>
    </div>
    @*<form class="form-group form-inline search pull-right">
            <div>
                <input type="text" name="txtSearch" placeholder="Tìm kiếm..." ng-model="vm.k" ng-blur="vm.search()" ng-keydown="$event.keyCode === 13 && vm.search()" class="form-control input-sm" />
                <button type="button" ng-click="vm.search()" class="btn-sm btn btn-primary">Tìm </button>
            </div>
        </form>
        <button class="btn btn-sm btn-success" type="button" ng-click="vm.add()">Tạo đơn hàng</button>
        <table class="table table-striped table-bordered ">
            <tr>

                <th ng-click="vm.sortBy('CustomerName',vm.reverse)" class="text-left">Tên khách hàng <i class="fas fa-sort"></i></th>
                <th ng-click="vm.sortBy('CustomerAddress',vm.reverse)" class="text-left">Địa chỉ <i class="fas fa-sort"></i></th>
                <th ng-click="vm.sortBy('CustomerPhone',vm.reverse)" class="text-left">Số điện thoại <i class="fas fa-sort"></i></th>
                <th ng-click="vm.sortBy('TotalMoney',vm.reverse)" class="text-right">Tổng tiền <i class="fas fa-sort"></i></th>
                <th ng-click="vm.sortBy('DateOrder',vm.reverse)" class="text-right">Ngày đặt hàng <i class="fas fa-sort"></i></th>
                <th ng-click="vm.sortBy('DateCreated',vm.reverse)" class="text-right">Ngày tạo<i class="fas fa-sort"></i></th>
                <th>
                </th>
            </tr>
            <tr ng-repeat="item in vm.orders|orderBy:vm.sortColumn:vm.reverse">

                <td>{{item.CustomerName}}</td>
                <td>{{item.CustomerAddress}}</td>
                <td>{{item.CustomerPhone}}</td>
                <td class="text-right">{{(item.TotalMoney|number)+" VNĐ"}}</td>
                <td class="text-right">{{item.DateOrder|date:"dd/MM/yyyy"}}</td>
                <td class="text-right">{{item.DateCreated|date:"dd/MM/yyyy"}}</td>
                <td>
                    <button class="btn btn-sm btn-info" type="button" ng-click="vm.info(item)">Xem</button>
                    <button class="btn btn-sm btn-warning" type="button" ng-click="vm.edit(item)">Sửa</button>
                    <button class="btn btn-sm btn-danger" type="button" ng-click="vm.remove(item)">Xóa</button>
                </td>
            </tr>
            <tr ng-if="!vm.orders||(vm.orders).length==0">
                <td colspan="8" class="text-center text-danger">
                    Không có dữ liệu!
                </td>
            </tr>
        </table>
        <div class="row">
            <div class="col-md-10">
                <ul uib-pagination total-items="vm.total" ng-model="vm.currentPage" max-size="5" items-per-page="vm.itemsPerPage" class="pagination-sm" ng-change="vm.onChangePagination()" num-pages="numPages"></ul>
            </div>
            <div class="col-md-2 form-group-sm pull-right" style="padding:20px;">
                <div class="breadcrumb small">Total record: {{vm.total}}</div>
            </div>
        </div>*@
    <div class="form-group">
        <select id="customer" name="customer" kendo-combo-box
                k-placeholder="'Search'"
                k-data-text-field="'Name'"
                k-data-value-field="'Id'"
                k-filter="'contains'"
                k-auto-bind="true"
                k-data-source="vm.dropdowns"
                ng-model="vm.dropdowns.Id"
                style="width:10%"
                ng-required="true">
        </select>
        <input type="search"
               id="searchBox" class="k-textbox"
               ng-model="vm.key" placeholder="Tìm kiếm..." style="width: 100%"
               ng-keydown="$event.keyCode === 13 && vm.search(vm.key,vm.dropdowns.Id)" />
        <button type="button" id="btnSearch"
                class="btn btn-sm btn-default"
                ng-click="vm.search(vm.key,vm.dropdowns.Id)">
            <i class="fas fa-search"></i>
        </button>
    </div>
    <div kendo-grid="vm.grid" options="vm.mainGridOptions"></div>
</div>
